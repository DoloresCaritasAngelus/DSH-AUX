# 视觉子代理策略:一次性 vs 长期

> 2026-08-14 与用户讨论后定稿;2026-08-14 晚实测修正(跨重启记忆结论推翻);2026-08-15 记忆落盘化(见"记忆库现状")。

## 实测结论(2026-08-14 晚)

1. **同会话内记忆 ✅**:同一 DSH 进程内,长期子代理跨轮次记住图片、支持"和刚才那张对比"(实测:追问 DeepSeek 圆点颜色,从记忆回答"红色")。
2. **跨 DSH 重启记忆 ❌**:重启后 cold-resume 恢复了会话身份(send_message 可投递),但**工具分析结果不在恢复的上下文里**——子代理自报"0 张,重启已清空记忆"。原"跟随主会话生命周期即可保留记忆"的假设不成立。
3. **focus hint 新版 ✅**:question 必填 + 任务聚焦 prompt 生效(具体问题"圆点颜色"→ 聚焦回答,不展开整页)。

**修正后的记忆架构**:
- **子代理 = 执行者**(每次带完整上下文分析,不依赖其跨重启记忆)
- **主会话(我)= 记忆库**:我的会话历史跨重启恢复(用户消息、工具结果都在我的日志里),由我维护"图片路径 → 已获取的描述摘要"清单;需要旧图对比时,把路径+旧摘要一并投喂子代理重新分析
- 即原"方案 C(备用)"转正为默认方案;方案 A 的"子代理自记忆"仅限同一进程会话内有效

**记忆库现状(2026-08-15)**:主会话记忆已落盘化——dsh-aux 每次 `vision_analyze` 成功后自动把 sessionId/attachmentId/问题/摘要/时间追加到 `~/.dsh/attachments/v1/image-memory.json`(有界 200 条),`/aux memory [n]` 随时可查最近分析记录;图片本身仍在附件对象库(content-addressed),重启后凭 attachmentId 可重投喂。主会话(我)无需再手工维护"路径→摘要"清单,直接查 `/aux memory` 即可;需要旧图对比时,用路径/attachmentId + 旧摘要重新分析。

## 现状:一次性子代理的代价

每次识图 = 新开一个前台一次性 subagent → 用 vision_analyze → 返回 → 销毁。

代价:
1. **初始化开销**:每次装配 agent 上下文、加载工具集(毫秒级~秒级,但重复发生)
2. **会话垃圾**:每次识图在 DSH 留下一个子代理 session 记录,长期堆积
3. **无状态**:子代理不记得上次看的图——"和刚才那张对比"做不到
4. **不可追问**:问完即焚,无法基于结果继续深挖(放大/区域/OCR 细化)

## 方案 A:长期 continuable 视觉子代理(已选定)

- **创建一次**:固定角色"视觉分析助手"(只做看图,中文结构化输出,记住历史)
- **每次识图**:send_message 投递任务,子代理作为下一 turn 执行,结果经通知返回
- **记忆**:子代理保留本会话所有看过的图,支持"和刚才那张对比"、"上次那张图的左上角"
- **生命周期**:跟随主会话——主会话结束/重启后重建(懒创建:首次识图时若不存在则创建)
- **容错**:send_message 失败(子代理丢失/会话过期)→ 自动重建再投递

## 方案 B(放弃):直接调 API

不用子代理,run_code 里直接 fetch 视觉模型 API。
放弃原因:
- 绕过了 DSH 的 LlmRuntime/能力门/事件记录/降级链——与 dsh-aux 的设计哲学(统一路由、可观测)冲突
- 需要自己维护 API key、错误处理、模态校验——重复造轮子
- 没有子代理的会话记忆(虽然主会话也可以记,但子代理作为独立上下文更干净)

## 方案 C(备用):子代理 + 主会话缓存

如果长期子代理的上下文膨胀成为问题(看的图太多),退化方案:
- 子代理只保留"最近 N 张图"的描述摘要
- 主会话(我)维护完整图片清单,需要旧图对比时重新投喂

## 实施要点

1. 子代理角色 prompt 固定、精简(避免它跑偏做别的)
2. 识图消息格式固定:imagePath + question,便于子代理稳定执行
3. 生命周期钩子:首次识图检查子代理是否存在(subagentId 存哪?)——主会话重启后 subagentId 失效,需要重新创建
4. 失败降级:send_message 失败 → 重建子代理 → 重试一次 → 仍失败则退回一次性方案

## 待确认(已解决)

- **subagentId 持久化**:DSH 的 continuable 子代理可 cold-resume(send_message 重启后仍可投递,已实测 ✅),**但工具结果上下文不恢复**(记忆丢失,见上)。实践中:subagentId 记在 `aux/.vision-agent.id`;首次识图读取,不存在/send_message 失败则重建。**记忆由主会话维护**(见"修正后的记忆架构")。
- 子代理上下文膨胀的阈值:由于记忆职责已移到主会话,子代理按次投喂,膨胀问题基本消除(每次消息自带完整上下文)。
